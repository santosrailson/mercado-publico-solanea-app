version: '3.8'

services:
  app:
    build: .
    ports:
      - "8002:8002"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/mercado.db
      - SECRET_KEY=solanea_mp_secret_2026_change_in_prod
      - ALLOWED_ORIGINS=*
    restart: unless-stopped
